import { Route } from "../Route/Route";
export declare class RouteFactory {
    private static allowedMethods;
    static createRoute(_method: string, path: string, callback: Function, controllerCtor: new () => any, schema?: any): Route;
    private static isMethodSupported(method);
}
