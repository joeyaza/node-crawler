"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _ = require("lodash");
var Route_1 = require("../Route/Route");
var RouteFactory = (function () {
    function RouteFactory() {
    }
    RouteFactory.createRoute = function (_method, path, callback, controllerCtor, schema) {
        var method = _method.toUpperCase();
        if (!RouteFactory.isMethodSupported(method)) {
            throw new Error("Method: " + method + " is not supported");
        }
        return new Route_1.Route(method, path, callback, controllerCtor, schema);
    };
    RouteFactory.isMethodSupported = function (method) {
        return !!(_.find(RouteFactory.allowedMethods, function (supportedMethod) {
            return supportedMethod === method;
        }));
    };
    return RouteFactory;
}());
RouteFactory.allowedMethods = ["GET", "POST", "PUT", "DELETE", "HEAD", "OPTIONS"];
exports.RouteFactory = RouteFactory;
