"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _ = require("lodash");
var Collection = (function () {
    function Collection() {
        this.collection = [];
    }
    Collection.prototype.add = function (item) {
        if (!this.itemExists(item)) {
            return this.collection.push(item);
        }
        throw new Error("Duplicate: " + item);
    };
    Collection.prototype.each = function (callback) {
        return _.each(this.collection, callback);
    };
    Object.defineProperty(Collection.prototype, "length", {
        get: function () {
            return this.collection.length;
        },
        enumerable: true,
        configurable: true
    });
    Collection.prototype.itemExists = function (item) {
        return !!(_.find(this.collection, function (searched) {
            return searched == item;
        }));
    };
    return Collection;
}());
exports.Collection = Collection;
