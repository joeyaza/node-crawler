import { Injector } from "@onscroll/di-ts";
import { Router } from "../Router/Router";
import { RestifyProvider } from "./RestifyProvider";
export declare class RestServer {
    private injector;
    private router;
    private restifyProvider;
    config: any;
    private restifyServer;
    constructor(injector: Injector, router: Router, restifyProvider: RestifyProvider);
    use(...args: any[]): any;
    start(controllers: any, callback: Function): void;
    private handleHealthRoute(request, response);
    private getStatus();
}
