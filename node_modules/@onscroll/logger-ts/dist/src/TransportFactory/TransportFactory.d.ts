import { Injector } from "@onscroll/di-ts";
import { AbstractTransport } from "./Transports/AbstractTransport";
import { LogLevel } from "../Logger/Logger";
export declare class TransportFactory {
    private injector;
    constructor(injector: Injector);
    createTransport(type: string, settings: any, logLevel?: LogLevel): AbstractTransport;
}
