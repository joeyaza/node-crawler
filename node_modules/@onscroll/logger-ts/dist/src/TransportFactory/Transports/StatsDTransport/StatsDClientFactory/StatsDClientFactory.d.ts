/// <reference types="statsd-client" />
import * as StatsDClient from "statsd-client";
export declare class StatsDClientFactory {
    createClient(settings: any): StatsDClient;
    private validate(settings);
}
