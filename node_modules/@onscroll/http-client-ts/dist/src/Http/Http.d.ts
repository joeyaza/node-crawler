import { NodeCacheFactory } from "../NodeCacheFactory/NodeCacheFactory";
import { RequestPromiseProvider } from "../RequestPromise/RequestPromiseProvider/RequestPromiseProvider";
import * as Promise from "bluebird";
export declare class Http {
    private nodeCacheFactory;
    private requestPromiseProvider;
    private _config;
    private requestPromise;
    private cache;
    constructor(nodeCacheFactory: NodeCacheFactory, requestPromiseProvider: RequestPromiseProvider);
    config: any;
    get(...args: any[]): Promise<any>;
    post(...args: any[]): Promise<any>;
    private getResponse(method, args);
    private getFromCache(key);
    private saveToCache(key, cachedPromise);
}
