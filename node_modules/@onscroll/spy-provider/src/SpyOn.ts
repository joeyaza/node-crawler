
import {SpyProvider} from "./SpyProvider";

export function SpyOn(target: any, key: string, descriptor: any) {

    let spy = SpyProvider.createSpy(target, key);

    descriptor.value = spy;

    return descriptor;
}